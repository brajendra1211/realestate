# API Requirements & Specifications

> **Note**: As requested, all API specifications, endpoints, request schemas, and responses across all panels have been unified into a single master document:
>
> 👉 **[docs/api-reference.md](file:///c:/Users/chira/UIS/realestate/docs/api-reference.md)**
>
> Please refer to `docs/api-reference.md` for the single source of truth organized cleanly by:
> 1. **Admin Panel APIs** (`/api/admin/*`)
> 2. **Agent Panel APIs** (`/api/agent/*`)
> 3. **Buyer / Customer Panel APIs** (`/api/buyer/*`, `/api/listings/*`, `/api/agreements/*`)
> 4. **Investor Panel APIs** (`/api/investor/*`)
> 5. **Public & Shared Services APIs** (`/api/geo/*`, `/api/upload/*`, `/api/auth/*`, `/api/rate/*`, `/api/leaderboard`)
